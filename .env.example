# Docker Compose Environment Variables
# Copy this file to .env and adjust the values as needed
# .env file is git-ignored and won't be committed

# =============================================================================
# Proxy Configuration (Optional)
# =============================================================================
# Uncomment and set these if you need to use a proxy for network access
# This is particularly useful for developers in regions with restricted access

# HTTP_PROXY=http://127.0.0.1:7897
# HTTPS_PROXY=http://127.0.0.1:7897
# http_proxy=http://127.0.0.1:7897
# https_proxy=http://127.0.0.1:7897

# If you set the proxy, you may also want to specify no_proxy domains:
# NO_PROXY=localhost,127.0.0.1,::1,.local
# no_proxy=localhost,127.0.0.1,::1,.local

# =============================================================================
# Example: Common Proxy Configurations
# =============================================================================

# Example 1: Local proxy (Clash, V2Ray, etc.)
# HTTP_PROXY=http://127.0.0.1:7897
# HTTPS_PROXY=http://127.0.0.1:7897

# Example 2: Corporate proxy
# HTTP_PROXY=http://proxy.company.com:8080
# HTTPS_PROXY=http://proxy.company.com:8080

# Example 3: Authenticated proxy
# HTTP_PROXY=http://username:password@proxy.example.com:8080
# HTTPS_PROXY=http://username:password@proxy.example.com:8080

# =============================================================================
# Notes
# =============================================================================
# - By default (without .env file), no proxy is used
# - Docker Compose will automatically load .env file if it exists
# - CI/CD environments (GitHub Actions) don't need proxy configuration
# - Make sure your proxy is running before starting Docker containers
